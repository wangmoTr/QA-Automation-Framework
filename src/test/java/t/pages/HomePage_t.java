package t.pages;

import POM.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class HomePage_t extends BasePage {
    //Locators

    public HomePage_t(WebDriver sentDriver) {
        super(sentDriver);
    }

}
