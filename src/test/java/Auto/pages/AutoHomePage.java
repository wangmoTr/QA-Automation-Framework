package Auto.pages;

import org.openqa.selenium.WebDriver;

public class AutoHomePage extends AutoBasePage {
    //Locators

    public AutoHomePage(WebDriver sentDriver) {
        super(sentDriver);
    }

}
