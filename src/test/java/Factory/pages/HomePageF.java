package Factory.pages;

import POM.pages.BasePage;
import org.openqa.selenium.WebDriver;

public class HomePageF extends BasePage {
    //Locators

    public HomePageF(WebDriver sentDriver) {
        super(sentDriver);
    }

}
