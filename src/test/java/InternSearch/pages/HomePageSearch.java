package InternSearch.pages;

import org.openqa.selenium.WebDriver;

public class HomePageSearch extends BasePageSearch {
    public HomePageSearch(WebDriver sentDriver) {
        super(sentDriver);
    }

}
